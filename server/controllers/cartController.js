const swag = require("../models/swag");

module.exports = {
  add: (req, res) => {
    //responsible for making sure swag isn't already in cart
  },

  delete: (req, res) => {},

  checkout: (req, res) => {}
};
